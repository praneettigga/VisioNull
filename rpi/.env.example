# VisioNull RPi — Environment Configuration
# Copy this file to .env and adjust values as needed:
#   cp .env.example .env

# Replace <LAPTOP_IP> with your laptop's actual IP address (run `hostname -I` on laptop)
VISIONULL_WEBHOOK_URL=http://<LAPTOP_IP>:5000/webhook
